# Lessons

- Exec is allowed by default unless a task explicitly forbids it. Do not assume no-shell/no-exec constraints unless stated.
- Even with a “no shell/commandes” constraint, git commit/push are allowed unless explicitly prohibited.
- When user questions list processing, restate which LIST.md was processed vs pending; verify current LIST.md state before claiming completion.

