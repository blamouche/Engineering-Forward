# Todo — veille-ia

## Context
- Trigger: cron `Daily veille IA Extraire urls de gmail`
- Goal: collect URLs from Gmail label `0---veille-ia`, sync/dedupe/update `LIST.md`, remove non AI/app-dev URLs, trash processed emails, then commit/push with prompt-hub bookkeeping.

## Plan
- [x] Load prompt-hub context (`lessons.md`, `memory.md`, `releases.md`).
- [x] Inspect Gmail messages and extract candidate article URLs.
- [x] Ensure repo is clean/synced; if not, commit/push all pending local changes first.
- [x] Update `LIST.md` with normalized deduped relevant URLs only.
- [x] Trash processed Gmail messages.
- [x] Update prompt-hub memory/version/releases and commit+push.

## Notes
- Follow `agents.md` `add-url` rules plus the cron-specific cleanup request.

## Review
- Found 2 Gmail messages in label `0---veille-ia`.
- Extracted and kept 7 relevant AI/app-dev URLs after filtering newsletter/redirect/non-relevant links.
- `LIST.md` was empty before update; 7 URLs added after normalization/dedupe; 0 existing URLs removed as non-relevant.
- Trashed 2 processed Gmail messages.
- Repo was cleaned by committing/pushing prompt-hub initialization first, then `LIST.md`, then final task logs.
